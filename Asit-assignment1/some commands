python -m venv venv

conda activate venv

pip install -r requirements.txt

#If python installed with conda

conda streamlit run app.py

#if python is directly installed

python -m streamlit run app.py